hl.on("hyprland.start", function()
	hl.exec_cmd("systemctl --user enable dunst;systemctl --user start dunst")
	hl.exec_cmd("systemctl --user enable hypridle;systemctl --user start hypridle")
	hl.exec_cmd("systemctl --user enable hyprpaper;systemctl --user start hyprpaper")
	hl.exec_cmd("ashell")
	hl.exec_cmd("wl-paste --type text --watch cliphist store & disown")
	hl.exec_cmd("wl-paste --type image --watch cliphist store & disown")
end)
