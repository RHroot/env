hl.on("hyprland.start", function()
	hl.exec_cmd("dbus-update-activation-environment --systemd --all")
	hl.exec_cmd("waypaper --restore")
	hl.exec_cmd("systemctl --user start hyprpolkitagent")
	hl.exec_cmd("pkill -f dunst && dunst & disown")
	hl.exec_cmd("pkill -f hypridle && hypridle & disown")
	hl.exec_cmd("pkill -f nm-applet && nm-applet & disown")
	hl.exec_cmd("eww kill; eww daemon & sleep 0.5; eww open hyprbar")
	hl.exec_cmd("wl-paste --type text --watch cliphist store & disown")
	hl.exec_cmd("wl-paste --type image --watch cliphist store & disown")
	-- hl.exec_cmd("systemctl restart --user emacs.service")
end)
