###################################
# General & Window Settings
###################################
# Launch in fullscreen by default
fs=yes

# Keep player open after video finishes
keep-open=yes

# Save playback position on quit
save-position-on-quit=yes

# Hide cursor automatically after 1 second in fullscreen
cursor-autohide=1000

###################################
# Subtitle Positioning & Style
###################################
# Position flush right above the bottom edge
sub-use-margins=yes
sub-pos=103
sub-margin-y=30

# Scales override on custom/ASS subtitles
sub-ass-override=scale

# Language defaults (prioritize English)
slang=eng,en
alang=eng,en

# Visuals: Bold yellow text, deep black border, depressed drop shadow
sub-font="Noto Sans"
sub-bold=yes
sub-font-size=40
sub-color="#FFE000"
sub-border-color="#000000"
sub-border-size=3.0

# Depressed heavy drop-shadow effect for ultra-high contrast
sub-shadow-offset=3.0
sub-shadow-color="#000000"

# Explicitly disable background box/fill so only text renders
sub-back-color="#00000000"

# Auto-load external subtitle files matching video name
sub-auto=fuzzy

###################################
# Video Output & Rendering
###################################
vo=gpu-next
gpu-api=opengl
hwdec=auto-safe

# Tone-mapping for HDR to SDR
tone-mapping=bt.2446a
target-colorspace-hint=no

# but uses significantly less GPU power, leaving headroom for 4K video.
profile=high-quality
scale=spline36
cscale=spline36
dscale=mitchell

# anime skies but prevents your integrated GPU from choking on high-bitrate files.
deband=yes
deband-iterations=1
deband-threshold=35
deband-range=16
deband-grain=5

# Display color management
target-prim=auto
target-trc=auto
video-output-levels=full
dither-depth=auto

# Reverting to audio sync guarantees the player will never deadlock.
video-sync=audio

###################################
# Audio & Screenshots
###################################
# Auto-load external audio files
audio-file-auto=fuzzy

# Allow volume boost up to 200%
volume-max=200

# High-quality PNG screenshots saved to Pictures
screenshot-format=png
screenshot-high-bit-depth=yes
screenshot-directory="~/Screenshots"
