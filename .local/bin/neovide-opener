#!/bin/sh

if [[ "$*" =~ ^([^:]+):([0-9]+)(:?.*)$ ]]; then
  file="${BASH_REMATCH[1]}"
  line="${BASH_REMATCH[2]}"
  exec neovide --no-tabs --reuse-instance --fork "$file" --remote-send ":${line}<CR>"
else
  exec neovide --no-tabs --reuse-instance "$@"
fi
